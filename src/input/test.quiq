struct Test {
  thing: bool;
}

struct Obj {
  action: fn<Test, int>;
}

Obj o = <Obj>{
  action: func (bool thing) int {
    print("working!!!");
    return 10;
  }
}

int t = o.action(true);
print(t);
